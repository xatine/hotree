import styled from 'styled-components';

export const Container = styled.div`
  background: var(--primary2);
  height: 12vh;
  border-top: 2vh solid var(--primary1);
  position: relative;
`;

export const Title = styled.h2`
  position: absolute;
  font-size: 36px;
  color: var(--white);
  left: 250px;
  padding: 20px 0;
  font-weight: 300;
`;
